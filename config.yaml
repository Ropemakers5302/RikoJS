ai:
  enabled: false
  provider: "openai"
  api_key: ""
  api_base: "https://api.openai.com/v1"
  model: "gpt-4"

scan:
  threads: 25
  timeout: 10
  max_retries: 3

output:
  verbose: false
  save_results: true
  output_file: "results.json"
